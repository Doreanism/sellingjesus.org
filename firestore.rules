rules_version = '2';

// Order data is only ever touched by the functions, which use the admin SDK and bypass these
// rules. Nothing here should be reachable by a client, so deny everything.
service cloud.firestore {
    match /databases/{database}/documents {
        match /{document=**} {
            allow read, write: if false;
        }
    }
}
